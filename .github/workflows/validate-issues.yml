name: Validate Issues

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  validate:
    uses: peavers-warcraft/workflow-templates/.github/workflows/validate-issues.yml@master
    secrets: inherit
